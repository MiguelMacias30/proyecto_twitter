<p id="notice"><%= notice %></p>


<p>
  <strong>Name:</strong>
  <%= @tweet.user.name %>
</p>

<p>
  <strong>contenido:</strong>
  <%= @tweet.content %>
</p>

<div class="imgredonda">
	<strong>imagen:</strong>
	<img src="<%= @tweet.user.image%>" width="50px" heigth="50px" alt="">
</div>
	
	<br>
	<br>
	<div>
		<p class="seg">followers:</p><%=@tweet.likes.count%><p class="seg">seguidores</p>
		<%@tweet.likes.each do |like| %>
		<span class="imgredonda"><%= image_tag like.user.image, width:"4%" %></span>
		<% end %>
	</div>
<br>

<%= link_to 'Edit', edit_tweet_path(@tweet) %> |
<%= link_to 'Back', tweets_path %>
